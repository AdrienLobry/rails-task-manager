<h2><%= @task.title %></h2>
<p><input type="checkbox" <%= "checked" if @task.completed %> disabled><%= @task.details %></p>
<%= link_to "Delete",
  task_path(@task),
  data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}
%>

<%= link_to "Edit", edit_path %>

<%= link_to "Back to list", tasks_path %>
